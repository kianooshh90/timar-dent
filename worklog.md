---
Task ID: 1
Agent: Z.ai Code (main)
Task: طراحی وب‌سایت معرفی شیک و منحصربه‌فرد برای مجموعه «تیمار» (TIMAR DENTAL CENTER / TDC) با HTML/CSS/JS خالص + Three.js

Work Log:
- بررسی ساختار پروژه Next.js و dev.log؛ سرور dev روی پورت 3000 در حال اجرا بود.
- بارگذاری اسکیل image-generation و تولید ۶ تصویر (۲ عکس بنیان‌گذاران: کیانوش حیدری، سوما رحمانی + ۴ thumbnail نمونه‌کار: crown, bridge, implant, veneer) در public/TDC-Intro/assets/.
- ساخت logo.svg: مونوگرام فوق‌مینیمال TDC با ساقه‌ی مشترک بین T و D (قفل‌شده)، bow خاکستری برای D، C نارنجی قوس‌دار + نقطه‌ی تاکید. گرادیان نارنجی #F48C06→#E85D04.
- نوشتن index.html: ساختار سمنتیک، dir="rtl" lang="fa"، ۷ بخش (نوار ناوبری شیشه‌ای، Hero، درباره ما/بنیان‌گذاران، گالری ۳بعدی، فرم لید، فرم آپلود، فوتر)، importmap برای Three.js از CDN، فونت Vazirmatn + Sora.
- نوشتن style.css: سیستم طراحی کامل با کامنت‌بندی (توکن‌ها، ریست، تایپوگرافی، دکمه‌ها+ریپل، نوار شیشه‌ای، Hero با glow و grid، کارت بنیان‌گذاران، گالری، فرم‌ها، dropzone، فوتر چسبان، مودال ۳بعدی، toast، انیمیشن‌های reveal، ریسپانسیو mobile-first، reduced-motion).
- نوشتن script.js: ماژول ES با import Three.js + OrbitControls؛ رندر گالری از داده؛ Viewer سه‌بعدی با ۴ مدل پروسیجرال (Crown لاته، Bridge سه‌واحدی، Implant با پیچ+اباتمنت+کرن، Veneer پوسته‌ای)؛ کنترل مودال (Escape/backdrop/X/reset دوربین)؛ اعتبارسنجی کامل دو فرم با پیام‌های فارسی؛ dropzone با drag/drop؛ ریپل؛ منوی موبایل؛ نوار اسکرول + هایلایت لینک فعال؛ IntersectionObserver برای reveal.
- راه‌اندازی پیش‌نمایش: src/app/page.tsx به redirect('/TDC-Intro/index.html') تغییر یافت تا مسیر / سایت استاتیک را سرو کند.
- اعتبارسنجی نهایی با Agent Browser: redirect کار می‌کند (307→200)، همه ۶ تصویر لود می‌شوند، ۵ بخش + ۴ کارت نمونه‌کار + ۲ کارت بنیان‌گذار رندر شد، مودال ۳بعدی با WebGL باز می‌شود و مدل کرن رندر می‌شود، اعتبارسنجی هر دو فرم (خطاها + موفقیت) کار می‌کند، آپلود فایل کار می‌کند، منوی همبرگری موبایل باز می‌شود، فوتر چسبان در پایین قرار دارد، بدون خطای کنسول.
- تحلیل بصری با VLM: hero «بسیار شیک و مدرن، هم‌تراز با برندهای بین‌المللی» تایید شد؛ مدل ۳بعدی کرن سفید براق روی پلتفرم تیره رندر شد.
- bun run lint: بدون خطا.

Stage Summary:
- خروجی نهایی در پوشه public/TDC-Intro/ شامل: index.html (25KB), style.css (31KB), script.js (23KB), logo.svg (1.4KB), assets/ (۶ تصویر PNG).
- سایت از طریق مسیر / در Preview Panel قابل مشاهده است (redirect خودکار به /TDC-Intro/index.html).
- همه‌ی تعاملات کلیدی (ویوور ۳بعدی، فرم‌ها، آپلود، منوی موبایل، فوتر چسبان) در مرورگر تایید شدند.
- فایل‌ها کاملاً مستقل هستند (HTML/CSS/JS خالص + Three.js از CDN) و قابل انتقال به هر هاستی.
